{
  "name": "data4less-api",
  "version": "1.0.0",
  "description": "Serverless API for Data4Less platform - Paystack integration & Firebase Admin",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
